---
"@entellix/contracts": patch
"@entellix/core": patch
"@entellix/instructions": patch
"@entellix/standalone": patch
---

Add complete TSDoc contracts to TypeScript modules, classes, and functions, enforce them in repository checks, and refresh standalone runtime and repository toolchain dependencies.
